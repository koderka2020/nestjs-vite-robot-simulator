{
  "name": "nestjs-vite-robot-simulator",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}